---
title: "RSE Workbench Build Container"
subtitle: "Pytask Config"
format: gfm
filters:
    - sorting-hat
    - ripper
extensions:
  ripper:
    output-name: "config"
    output-dir: "../../../src/projects/prospectorDHSNepal/tasks"
  sorting-hat:
        keep: python
execute:
  eval: false
---

This notebook is a record of the `pytask` configuration for the Prospector: DHSNepal 
project. `pytask` requires a simple configuration file at its src base
to manage the build directories for a project.


```{python}
from pyprojroot import here
from pathlib import Path

SRC = here() / "src/projects/prospectorDHSNepal/tasks"
BLD = here() / "notebooks/projects/prospectorDHSNepal/pytask_build"
```

Any other configs can be added to this file.