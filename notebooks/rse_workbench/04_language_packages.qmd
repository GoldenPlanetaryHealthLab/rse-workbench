---
title: "04 Data Science Language Package Layer"
format: gfm
execute:
  eval: false
filters:
  - sorting-hat
  - ripper
extensions:
  ripper:
    output-name: "datascience"
    output-dir: "../../src/rse_workbench"
  sorting-hat:
    keep: python
---

This notebook writes `rproject.toml` and `pyproject.toml`.

Do not run `rv sync` or `uv sync` here.

```{python}
from rse_workbench.datascience import (
    default_pyproject,
    default_rproject,
    write_pyproject_toml,
    write_rproject_toml,
)
```

```{python}
#| sorting-hat: keep
DEFAULT_R_PACKAGES = [
    "box",
    "remotes",
    "rprojroot",
    "stringi",
    "targets",
    "tarchetypes",
    "usethis",
    "httpgd",
    "httpuv",
    "parsermd",
    "rmarkdown",
    "skimr",
    "DiagrammeR",
    "svglite",
    "later",
    "rstudioapi",
    "dplyr",
    "languageserver",
    "here",
    "fledge",
    "readr",
    "stringr",
    "withr",
    "readxl",
    "tidyverse",
    "websocket",
    "codetools",
    "quarto",
    "rlang",
    "purrr",
    "tibble",
    "devtools",
    "jsonlite",
    "sf",
    "nanoparquet",
    "janitor",
    "lubridate",
    "frictionless",
    "pkgsite",
    "rdhs",
]

DEFAULT_R_GIT_DEPENDENCIES = [
    {
        "name": "sess",
        "git": "https://github.com/REditorSupport/vscode-R",
        "branch": "master",
        "directory": "sess",
    }
]

DEFAULT_PYTHON_PACKAGES = [
    "numpy",
    "pandas",
    "ipykernel",
    "jupyterlab",
]
```

```{python}
#| sorting-hat: remove
rproject = default_rproject(
    project_name=PROJECT_NAME,
    r_version=R_VERSION,
    packages=DEFAULT_R_PACKAGES,
    git_dependencies=DEFAULT_R_GIT_DEPENDENCIES,
)

pyproject = default_pyproject(
    project_name=PROJECT_NAME,
    python_version=PYTHON_VERSION,
    packages=DEFAULT_PYTHON_PACKAGES,
)

write_rproject_toml(RPROJECT_TOML_PATH, rproject)
write_pyproject_toml(PYPROJECT_TOML_PATH, pyproject)
```

```{python}
#| sorting-hat: remove
print(RPROJECT_TOML_PATH.read_text())
print(PYPROJECT_TOML_PATH.read_text())
```

Stop here. The next notebook writes the runtime activation contract.
